// The admin area has its own password protection via AdminGuard component

// No middleware needed for public company profile website
// Admin area is protected by AdminGuard component with simple password

export const config = {
  matcher: [],
}
